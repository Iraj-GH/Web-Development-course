<!DOCTYPE html>
<html>
<body>
    <p>Finding the largest number with help of Arguments Object</p>
    <p id="result"></p>
    <script>
    function findMax() {
        var max = -Infinity;
        for (var i = 0; i < arguments.length; i++) {
            if (arguments[i] > max) {
                max = arguments[i];
            }
        }
        return max;
    }
    document.getElementById("result").innerHTML = findMax(8, 4, 5, 6, 2);
    </script>
</body>
</html>
